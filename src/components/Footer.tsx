import Link from 'next/link';
import { useRouter } from 'next/router';

const Footer: React.FC = _ => {
    const router = useRouter();
    return (
        <div className='w-full p-2'>
            Hiiiiiiiii
        </div>
    )
}

export default Footer;
