name: Run Tests

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: 🏁 Checkout repo
        uses: actions/checkout@v3

      - name: 🧪 Test our application
        # Directory name only
        uses: ./.github/actions/test
